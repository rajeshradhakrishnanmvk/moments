version: 2.1

jobs:
  build:
   filters:
     branches:
       only:
         - momentsspace
   machine: true
   steps:
     - checkout
     - sonarcloud/scan

orbs:
  sonarcloud: sonarsource/sonarcloud@1.0.1

workflows:
  main:
    jobs:
      - build:
          context: SonarCloud
